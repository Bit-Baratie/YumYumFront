import React from "react";
import Load from "./Loading.module.scss";

const Loading = () => {
  return (
    <div>
      <span className={Load.loader2}></span>
    </div>
  );
};

export default Loading;
